# Optionnel — laisser vide pour que le site fonctionne sans persistance des avis (comportement identique à la maquette : les avis restent en mémoire le temps de la session du visiteur).
# Renseigner ces deux valeurs (depuis Project Settings > API sur supabase.com) pour activer la persistance réelle des avis.
# Voir README.md > "Avis clients / Supabase" pour le schéma de table et les policies RLS à créer.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
